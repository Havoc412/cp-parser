// 测试Mini语言的示例程序
// 包含算术表达式、赋值语句、条件语句、循环等

int main() {
    int a = 10;
    int b = 20;
    double c = 3.14;
    
    // 算术表达式测试
    int result = a + b * 2;
    
    // 条件语句测试
    if (a < b) {
        result = result + 1;
    } else {
        result = result - 1;
    }
    
    // 错误测试 - 非法数字格式
    double invalid = 3..14;
    
    // 循环测试
    while (a > 0) {
        a = a - 1;
        b = b + a;
    }
    
    // 数组访问测试
    int arr[10];
    arr[0] = 100;
    
    return 0;
}
# 文件结束标记 